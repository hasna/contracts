# @hasna/contracts-client

Opt-in authenticated HTTP transport and storage client formerly published by
`@hasna/contracts/client`. Network and environment behavior lives here, never
in the core contract import graph.
