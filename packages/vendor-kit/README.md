# @hasna/contracts-vendor-kit

Opt-in source generator formerly published by `@hasna/contracts/vendor-kit`.
It reads and writes repositories by design and therefore has a separate
package boundary from `@hasna/contracts`.
