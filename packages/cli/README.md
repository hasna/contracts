# @hasna/contracts-cli

Opt-in repository conformance, artifact scanning, and migration CLI formerly
shipped as binaries in `@hasna/contracts`. Its filesystem and process behavior
is isolated from the core schema package.
