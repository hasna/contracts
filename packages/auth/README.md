# @hasna/contracts-auth

Opt-in runtime auth helpers formerly published by `@hasna/contracts/auth`.
This package is intentionally separate because its stores, environment
resolution, and request middleware are not part of a pure data contract.
