{
  "$schema": "./src/hasna.contract.schema.json",
  "schema": "hasna.service_contract.v1",
  "name": "contracts",
  "class": "library",
  "contractVersion": "v1",
  "kitVersion": "1.0.0-rc.1",
  "description": "Side-effect-free shared schemas, inferred types, validators, and JSON Schema artifacts. Ships no CLI, store, network client, or code generator.",
  "bins": [],
  "hosting": [
    "user-hosted"
  ],
  "serviceSurfaces": [
    {
      "name": "package-sdk",
      "kind": "sdk",
      "status": "supported",
      "authMode": "none",
      "exportSubpath": "."
    }
  ],
  "metadata": {
    "release": {
      "artifactScan": {
        "script": "scan:artifact"
      }
    },
    "conformance": {
      "waivedSurfaces": [
        {
          "kind": "api",
          "reason": "Execution-free schema library; it does not own an HTTP service boundary."
        },
        {
          "kind": "mcp",
          "reason": "Execution-free schema library; MCP execution belongs to consuming applications."
        }
      ]
    }
  }
}
